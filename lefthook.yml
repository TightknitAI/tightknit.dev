pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{js,mjs,cjs,ts,tsx,astro,css,json,md,yml,yaml}'
      run: npx prettier --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: npm run typecheck
